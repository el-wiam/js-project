<?php
    $con=@mysqli_connect("localhost","root","","miniprojet") or die("ereur de connexion ");

    // $req= "SELECT * FROM formulaire";
    // $res=@mysqli_query($con,$req);
    // while($tab1=mysqli_fetch_assoc($res)){
